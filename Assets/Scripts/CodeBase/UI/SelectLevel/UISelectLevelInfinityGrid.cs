using UnityEngine;

namespace CodeBase.UI.SelectLevel
{
    public class UISelectLevelInfinityGrid : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
